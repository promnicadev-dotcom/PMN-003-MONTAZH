# PMN-003 MONTAZH v0.3.6 公開テンプレート完成テスト報告

## 位置づけ

v0.3.4〜0.3.5で実施した最終安定化を維持したまま、公開利用時にPromnica固有表現を強制しないための最小限のテンプレート一般化を実施した。
この版を機能完成候補とし、新機能開発は原則終了する。

## 変更したファイル
- engine.py
- app.py
- README.md
- README.txt
- TEST_REPORT_v0.3.6.md

## 実装した項目
1. ヘッダー編集欄を「1 録画とタイトル」へ移動。空欄で非表示。
2. 出力テーマを5プリセット化。自由色指定は導入せず、UI複雑化を回避。
3. テーマは背景・文字・アクセント・補助文字をまとめて変更。
4. テーマをプロジェクト固有設定 `theme_id` として保存・復元。
5. `.montazh` schemaを3へ更新。schema 2(v0.3.5)は「研究所ダーク」を補って小規模移行。
6. 既存の一般化タイトル解析を維持。PMNを必須にしない。
7. 詳細設定からヘッダー編集を削除し、主要な表示設定を最初の画面へ集約。

## テーマ
- 研究所ダーク
- モノクロ
- ライト
- ネイビー
- ウォーム

## 実施したテスト
- Python構文検査: engine.py / app.py PASS
- 5テーマすべてで1080x1920 overlay画像生成 PASS
- ヘッダーあり + 構造化タイトル `AX-12 CLEANER 重複整理装置` 描画 PASS
- ヘッダー空欄 + 自由タイトル `今日の開発記録` 描画 PASS
- schema 2プロジェクト相当データから theme_id=研究所ダーク への移行 PASS
- schema 3で header_text / theme_id の復元 PASS
- 未知theme_idの明示的拒否 PASS

## v0.3.4から維持している安定化項目
- `.montazh` Atomic Save
- MP4一時出力→検査→正式置換
- VOICEVOX timeout / HTTP異常処理
- FFmpeg進捗停止監視
- 素材消失検査
- schema管理
- busy制御
- Project A → B → A の状態分離

## 未解決・実機確認
- Windows実機で5テーマを切り替え、プレビューと実MP4の色が一致することを最終確認する。
- 保存→終了→再起動→`.montazh`読込後にヘッダーとテーマが復元されることをWindows GUIで確認する。
- v0.3.4時点から残る、実VOICEVOX/Nemoの異常系テストと「配置を確認」フリーズ再発監視は継続する。

## 仕様から逸脱した点
- なし。
- カラーピッカー等の自由色指定は意図的に実装せず、5プリセットに限定した。
